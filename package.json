{
  "name": "graphql-modules-platform",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "jest",
    "format": "prettier --write {packages,apps}/**/*.{js,json,css,md,ts,tsx}"
  },
  "devDependencies": {
    "@types/jest": "25.1.4",
    "bob-the-bundler": "0.3.4",
    "graphql": "14.6.0",
    "jest": "25.1.0",
    "prettier": "1.19.1",
    "reflect-metadata": "0.1.13",
    "ts-jest": "25.2.1",
    "typescript": "3.8.3"
  }
}
